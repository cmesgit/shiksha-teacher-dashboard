// components/SkillToastContext.js — split out of SkillToast.jsx so that file
// only exports the component (react-refresh/only-export-components).
import { createContext } from "react";

export const SkillToastContext = createContext(null);
